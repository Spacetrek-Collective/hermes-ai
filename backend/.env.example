# Port to listen on
PORT=8787

# Secret for signing JWTs — change to a long random string in production
JWT_SECRET=change-me-to-a-long-random-secret

# SQLite file path (relative to server/)
DB_PATH=hermes.db

# Comma-separated allowed origins for CORS (the frontend URL)
CORS_ORIGIN=http://localhost:7200
